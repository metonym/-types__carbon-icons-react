{
  "private": true,
  "scripts": {
    "build": "vitest run",
    "postbuild": "dprint fmt dist/**/*.d.ts --excludes src/**"
  },
  "devDependencies": {
    "@carbon/icons": "11.25.0",
    "@types/react": "^18.2.20",
    "dprint": "^0.40.2",
    "typescript": "^5.1.6",
    "vitest": "^0.34.1"
  }
}
